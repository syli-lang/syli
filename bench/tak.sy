signature:
  extern syli_print_i64 : int64 -> unit = "syli_print_i64"
end
let rec tak x y z =
  if y < x then
    tak (tak (x - 1) y z) (tak (y - 1) z x) (tak (z - 1) x y)
  else
    z
fn main () = syli_print_i64(tak 27 15 5)
